interface:
  display_name: "Media User Posts"
  short_description: "Retrieve creator content"
  default_prompt: "Use $media-user-posts to retrieve supported XHS, Douyin, Kuaishou, Bilibili, Zhihu, Instagram, X / Twitter, YouTube, TikTok, Weibo, or WeChat Channels creator content lists from platform-supported user IDs, usernames, channel URLs, profile URLs, short links, or share text. Supported input forms vary by platform."

policy:
  allow_implicit_invocation: true
